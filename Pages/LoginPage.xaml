<Page x:Class="pract7_trpo.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:pract7_trpo.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="450"
      Title="LoginPage">
    <StackPanel Orientation="Vertical" x:Name="LoginForm" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">
        <Label Content="ID"/>
        <TextBox Text="{Binding ID, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
        <Label Content="Пароль"/>
        <TextBox Margin="5" Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}"/>
        <Button
                Name="Login"
                Height="25" Width="150"
                VerticalAlignment="Bottom"
                Margin="5" Content="Войти" Click="Login_Click"/>
        <Button Height="25" Width="150"
                VerticalAlignment="Bottom"
                Margin="5" Content="Зарегистрироваться" Click="RegButton_Click" >
            
        </Button>
    </StackPanel>
</Page>
